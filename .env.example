# Doc-O-Matic Environment Variables
# Copy this file to .env and update with your actual values
# cp .env.example .env

# ============================================================================
# PostgreSQL Service Configuration (for docker-compose)
# ============================================================================

# PostgreSQL database name
POSTGRES_DB=docomatic

# PostgreSQL user
POSTGRES_USER=postgres

# PostgreSQL password (REQUIRED - change this in production!)
POSTGRES_PASSWORD=postgres

# PostgreSQL resource limits (optional)
POSTGRES_CPU_LIMIT=1.0
POSTGRES_MEMORY_LIMIT=512M
POSTGRES_CPU_RESERVATION=0.25
POSTGRES_MEMORY_RESERVATION=128M

# ============================================================================
# Doc-O-Matic Service Configuration
# ============================================================================

# Database connection URL (REQUIRED)
# For containerized setup, this connects to the PostgreSQL service
# Format: postgresql://user:password@postgresql:5432/dbname
DATABASE_URL=postgresql://postgres:postgres@postgresql:5432/docomatic

# Database connection pool settings (optional)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# SQL query logging (optional, set to "true" for debugging)
SQL_ECHO=false

# Logging level (optional)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Doc-O-Matic service resource limits (optional)
DOC_O_MATIC_CPU_LIMIT=1.0
DOC_O_MATIC_MEMORY_LIMIT=512M
DOC_O_MATIC_CPU_RESERVATION=0.25
DOC_O_MATIC_MEMORY_RESERVATION=128M

# ============================================================================
# Optional Configuration
# ============================================================================

# GitHub token for export functionality (optional)
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
GITHUB_TOKEN=

# ============================================================================
# Notes
# ============================================================================
#
# REQUIRED variables:
#   - POSTGRES_PASSWORD: Must be set (change from default in production!)
#   - DATABASE_URL: Must be set (default works for docker-compose setup)
#
# OPTIONAL variables:
#   - All other variables have defaults and can be omitted
#   - Resource limits are optional but recommended for production
#   - GITHUB_TOKEN is only needed if using GitHub export functionality
#
# For local development (non-containerized):
#   - Use SQLite: DATABASE_URL=sqlite:///./docomatic.db
#   - PostgreSQL: DATABASE_URL=postgresql://user:pass@localhost:5432/docomatic
#
# For containerized setup:
#   - Use the default DATABASE_URL format shown above
#   - It connects to the PostgreSQL service in docker-compose
#   - Service name "postgresql" resolves to the PostgreSQL container
